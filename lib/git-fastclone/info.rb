# Version string for git-fastclone
module GitFastClone
  # Human readable stuff
  module Info
    VERSION = '1.1.1'.freeze

    SUMMARY = 'git-clone --recursive on steroids!'.freeze
    DESCRIPTION = 'A git command that uses reference repositories and threading to quickly and
recursively clone repositories with many nested submodules'.freeze
  end
end
